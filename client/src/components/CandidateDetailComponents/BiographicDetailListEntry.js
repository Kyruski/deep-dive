import React, {Fragment} from 'react'

const Component = (props) => {
  return( 
    <Fragment/>
  )
};

export default Component;

//This entire component is likely not necessary given how split-up the data already is by the time it reaches the parent list.